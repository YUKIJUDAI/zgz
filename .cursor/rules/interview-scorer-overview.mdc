---
description: 面试评分器项目的核心上下文
alwaysApply: true
---

# 面试评分器项目说明

- 可运行应用位于 `main/`。
- `main/index.html` 包含完整的界面、题库、评分逻辑和本地持久化。
- `main/server.ts` 是本地使用的零依赖 Node 静态服务。
- `main/package.json` 保持最小化，只使用 Node 内置能力。
- 默认使用中文编写项目说明、规则、技能，以及与该项目相关的新增内容。
- 优先做小而直接的修改，不要默认引入框架、构建工具或额外依赖。
- 保持项目可以在 `main/` 目录内通过 `npm run dev` 或 `npm run start` 直接启动。
- 修改评分逻辑时，要保持当前产品目标不变：本地面试练习、逐题评分、整体反馈、可执行建议。
